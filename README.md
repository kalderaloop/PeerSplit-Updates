# PeerSplit Updates

This public repository contains signed PeerSplit Android release assets and the measured `update.json` manifest only.

The application source, signing material, passwords, tokens, OAuth data and cloud capability data are not stored here.

PeerSplit verifies the APK checksum, Android package name, version code and signing certificate before opening Android's system installer.
